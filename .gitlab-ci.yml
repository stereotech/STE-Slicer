stages:
  - build
  - test

image: ultimaker/cura-build-environment

buildSteSlicer:
  stage: build
  script:
    - docker/build.sh
  artifacts:
    paths:
      - build

testSteSlicer:
  stage: test
  script:
    - docker/test.sh
